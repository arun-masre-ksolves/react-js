import React from 'react';

function Props (p) {
    return (
        <div>
            <hr />
            <p>Hello {p.name} and contanct number is {p.contact}</p>
        </div>
    );
}

export default Props;